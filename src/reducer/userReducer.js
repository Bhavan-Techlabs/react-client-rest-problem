import { createSlice } from "@reduxjs/toolkit";

//TODO: Add initialState
const initialState = {};

export const counterSlice = createSlice({
  name: "counter",
  initialState,
  reducers: {
    //TODO: Add slice actions
  }
});

export default counterSlice.reducer;
